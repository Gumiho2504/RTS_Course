using UnityEngine;

namespace GumihoEditorTool
{
    /// <summary>
    /// Inherit this instead of <see cref="MonoBehaviour"/> to enable
    /// Gumiho tabs, foldouts, buttons, and validation in the Inspector.
    /// </summary>
    public abstract class GumihoBehaviour : MonoBehaviour
    {
    }
}
